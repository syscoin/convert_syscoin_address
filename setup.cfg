[metadata]
license = MIT
license_files = LICENSE

# Add other sections like [options] if needed, or append if file exists
# Ensure this content is added appropriately if setup.cfg already has content. 